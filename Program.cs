using System;
using System.Windows.Forms;

namespace SchedualApp
{
    // Program class intentionally left empty because WPF App.xaml is the application entry point.
    // This file exists to avoid project-missing-file errors and must not define Main.
    public static class WinFormsEntryPlaceholder { }
}
